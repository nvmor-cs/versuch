{
  "name": "eisenzeit",
  "version": "1.0.0",
  "description": "Eisenzeit – Trainingslog fürs Fitnessstudio (PWA + Android-App via Capacitor)",
  "private": true,
  "scripts": {
    "build": "node scripts/build-www.mjs",
    "sync": "npm run build && npx cap sync android",
    "android:debug": "npm run sync && cd android && ./gradlew assembleDebug",
    "android:release": "npm run sync && cd android && ./gradlew bundleRelease"
  },
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
